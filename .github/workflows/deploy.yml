name: Deploy Backend to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to VPS via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 159.89.145.196
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /var/www/smarthomework/BE_WEB_TIENG_ANH_50M
            git pull origin main
            npm install
            npx prisma generate
            pm2 reload smarthomework-be
